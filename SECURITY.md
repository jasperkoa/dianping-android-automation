# Security Policy

## Sensitive information

请不要在公开 Issue/PR 中提交：

- 账号凭据、手机号、验证码、Cookie、Token；
- ADB 设备序列号；
- 内网地址、个人目录和含身份信息的日志/截图。

本项目默认通过 USB ADB 工作。不要为了运行本项目而把 ADB TCP 端口直接暴露到不可信网络。

## Reporting

如果发现会导致任意命令执行、凭据泄漏或错误操作其他已连接设备的问题，请优先通过仓库维护者提供的私密安全报告渠道提交，而不是公开披露敏感 PoC 数据。
